
#include <stdio.h>

int main(void)
{
        char *s = "random string of any length????";
        printf("s = %s\n", s);

        int numbers[4] = {'a'};
        printf("numbers: %d %d %d %d\n",
               numbers[0], numbers[1],
               numbers[2], numbers[3]);

        return 0;
}
